{
  "name": "finstart",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd finstart/backend && npm run dev",
    "dev:frontend": "cd finstart/frontend && npm run dev",
    "seed": "cd finstart/backend && npm run seed",
    "install:all": "cd finstart/backend && npm install && cd ../frontend && npm install",
    "build": "cd finstart/frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
